import * as React from "react";
import { Button } from "@zeit-ui/react";

export const App = () => {
  return (
    <div>
      <h1>Hello React!</h1>
      <Button>My button</Button>
    </div>
  );
};
